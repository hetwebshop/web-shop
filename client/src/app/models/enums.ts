export enum Gender {
    Male = 'Male',
    Female = 'Female',
    Other = 'Other'
}

export enum JobPostStatus {
    Active = 1,
    Closed = 2
}

export enum AdvertisementTypeEnum {
    JobAd = 1,
    Service = 2
}